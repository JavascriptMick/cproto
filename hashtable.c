#include <stdio.h>
//implement a simple hash table 
//which maps char to char[]
#define HASHTABLESIZE 100
// hmm, needs to b an array of pointers to some structure which includes a char (key) and char[] value
char hashtable[HASHTABLESIZE];

int main(){
  // hashtable = "hello";
}

